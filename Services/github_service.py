# Class to serve all the request related to PR and sanity
class GithubService:
    def __init__(token, pullRequest, branch):
        self.token = token
        self.PR = PR

    def getAllTarget(pullRequest):
        # check whether the PR is valid or not, add try catch
        verifyPullRequest(pullRequest)
        # Use API

    def getLatestCommit(pullRequest):
        # Use API

    def getParentCommit(sha);
        # Use API

    def verifyPullRequest(pullRequest):    
        # Use API

    def getPullRequest(sha):
        # Use API    